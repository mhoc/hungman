
exports.wordkey = function(token) {
  return token + '-word'
}

exports.statekey = function(token) {
  return token + '-state'
}

exports.remainkey = function(token) {
  return token + '-remaining'
}
